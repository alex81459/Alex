<section id="contact" class="contact-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-10">
                <div class="contact-cta-shell">
                    <div class="text-center contact-title section-intro">
                        <span class="section-kicker">CONTACTO</span>
                        <h2 class="section-heading">Construyamos algo útil.</h2>
                        <p class="section-lead">¿Buscas incorporar un desarrollador a tu equipo o necesitas llevar un proyecto a producción?</p>
                        <p class="contact-intro">Trabajo en desarrollo de software, integración de sistemas, infraestructura y automatización.</p>
                        <div class="contact-disponibilidad"><span aria-hidden="true"></span><strong>DISPONIBLE PARA NUEVAS OPORTUNIDADES</strong><small>Remoto · Híbrido · Chile</small></div>
                        <div class="contact-acciones">
                          <a class="contact-accion-principal" href="mailto:alex81459@gmail.com"><i class="fas fa-envelope" aria-hidden="true"></i>Escribirme por correo</a>
                          <a class="contact-accion-secundaria" href="https://www.linkedin.com/in/alex-salinas-ponce-5276691a3" target="_blank" rel="noopener">LinkedIn <span aria-hidden="true">↗</span></a>
                        </div>
                    </div>

                    <div class="row justify-content-center g-4 contact-options-grid">
                        <div class="col-lg-5 col-md-6 col-12">
                            <div class="contact-card contact-option-card">
                                <div class="contact-option-head">
                                    <span class="contact-option-index">01</span>
                                    <h3 class="contact-option-title">OPORTUNIDADES PROFESIONALES</h3>
                                </div>
                                <p class="contact-option-tags">Full Stack · Backend · Infraestructura<br />DevOps · Integraciones</p>
                                <p class="contact-option-desc">Para equipos que buscan incorporar capacidad técnica estable.</p>
                                <div class="contact-option-links">
                                    <a class="contact-channel" href="https://www.linkedin.com/in/alex-salinas-ponce-5276691a3" target="_blank" rel="noopener" aria-label="LinkedIn: Alex Salinas Ponce">
                                        <i class="fab fa-linkedin" aria-hidden="true"></i><span>LinkedIn</span>
                                    </a>
                                    <a class="contact-channel" href="mailto:alex81459@gmail.com" target="_blank" rel="noopener" aria-label="Correo electrónico: alex81459@gmail.com">
                                        <i class="fas fa-envelope" aria-hidden="true"></i><span>Correo</span>
                                    </a>
                                </div>
                            </div>
                        </div>

                        <div class="col-lg-5 col-md-6 col-12">
                            <div class="contact-card contact-option-card">
                                <div class="contact-option-head">
                                    <span class="contact-option-index">02</span>
                                    <h3 class="contact-option-title">PROYECTOS / COLABORACIÓN</h3>
                                </div>
                                <p class="contact-option-tags">Software · APIs · Automatización<br />Arquitectura · Infraestructura</p>
                                <p class="contact-option-desc">Para proyectos, consultoría e integraciones puntuales.</p>
                                <div class="contact-option-links">
                                    <a class="contact-channel" href="mailto:alex81459@gmail.com" target="_blank" rel="noopener" aria-label="Correo electrónico: alex81459@gmail.com">
                                        <i class="fas fa-envelope" aria-hidden="true"></i><span>Correo</span>
                                    </a>
                                    <a class="contact-channel" href="https://github.com/alex81459" target="_blank" rel="noopener" aria-label="GitHub: alex81459">
                                        <i class="fab fa-github" aria-hidden="true"></i><span>GitHub</span>
                                    </a>
                                    <a class="contact-channel" href="https://t.me/alex81459" target="_blank" rel="noopener" aria-label="Telegram: alex81459">
                                        <i class="fab fa-telegram" aria-hidden="true"></i><span>Telegram</span>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="contact-about">
                        <div class="contact-about-identidad">
                            <img src="assets/img/frontalv2-circular.webp" width="400" height="400" loading="lazy" decoding="async" alt="Alex Salinas Ponce, Ingeniero Informático">
                            <div>
                                <strong>Alex Salinas Ponce</strong>
                                <small>Ingeniero Informático · Chile</small>
                            </div>
                        </div>
                        <div class="contact-about-copy">
                            <span class="contact-about-kicker">SOBRE MÍ</span>
                            <h3>La tecnología tiene sentido cuando resuelve algo real.</h3>
                            <p>Mi trabajo conecta el desarrollo de software con las personas y los procesos que lo necesitan. Participo desde el levantamiento de requerimientos y la arquitectura hasta el despliegue, la documentación y la mantención de cada solución.</p>
                            <p>He trabajado en educación pública, gestión municipal y producción industrial. Esa experiencia me permite entender tanto una plataforma digital como la infraestructura y la operación que la sostienen.</p>
                            <a class="contact-about-link js-scroll-trigger" href="#services">Cómo puedo aportar a tu equipo <span aria-hidden="true">→</span></a>
                        </div>
                    </div>
                    <p class="contact-privacy-note">Los datos que compartas se usarán únicamente para responder tu consulta. Consulta la <a href="https://alex81459.github.io/Alex/privacidad/">Política de Privacidad</a>.</p>
                </div>
            </div>
        </div>
    </div>
</section>
    <style>
        .contact-intro-support {
            opacity: 0.9;
            font-weight: 600;
        }

        .contact-privacy-note {
            max-width: 760px;
            margin: 16px auto 0;
            color: var(--texto-secundario);
            font-size: 0.92rem;
            line-height: 1.6;
        }

        .contact-privacy-note a {
            color: #8feaff;
            font-weight: 700;
            text-decoration: underline;
        }

        .contact-about { display: grid; grid-template-columns: 148px minmax(0, 1fr); align-items: center; gap: 30px; max-width: 880px; margin: 34px auto 0; padding: 24px; border: 1px solid var(--borde); border-radius: var(--radio-superficie); background: linear-gradient(135deg, color-mix(in srgb, var(--fondo-tarjeta-alternativo) 88%, transparent), color-mix(in srgb, var(--primario) 5%, transparent)); box-shadow: 0 14px 36px rgba(0, 0, 0, .12); text-align: left; }
        .contact-about-identidad { display: flex; flex-direction: column; align-items: center; gap: 11px; min-width: 0; text-align: center; }
        .contact-about-identidad img { display: block; width: 108px !important; height: 108px !important; max-width: 108px !important; aspect-ratio: 1; flex: 0 0 108px; object-fit: cover; border: 2px solid var(--borde-destacado); border-radius: 50%; box-shadow: 0 0 0 5px color-mix(in srgb, var(--primario) 9%, transparent); }
        .contact-about-identidad strong { display: block; color: var(--texto-principal); font-size: .78rem; line-height: 1.35; }
        .contact-about-identidad small { display: block; margin-top: 3px; color: var(--texto-secundario); font-size: .65rem; line-height: 1.4; }
        .contact-about-kicker { color: var(--primario); font: .58rem ui-monospace, SFMono-Regular, Consolas, monospace; letter-spacing: .1em; }
        .contact-about-copy h3 { margin: 7px 0 9px; color: var(--texto-principal); font-size: clamp(1.05rem, 1.7vw, 1.35rem); line-height: 1.25; }
        .contact-about-copy p { margin: 0 0 8px; color: var(--texto-secundario); font-size: .82rem; line-height: 1.6; }
        .contact-about-link { display: inline-flex; gap: 9px; margin-top: 4px; color: var(--primario); font-size: .76rem; font-weight: 700; text-decoration: none; }
        .contact-about-link:hover { text-decoration: underline; text-underline-offset: 4px; }
        .contact-title { max-width: 760px; margin-inline: auto; }
        .contact-title .section-heading { margin-bottom: 13px; text-transform: none; }
        .contact-intro { margin: 0 auto; max-width: 650px; color: var(--texto-secundario); font-size: .92rem; line-height: 1.65; }
        .contact-disponibilidad { display: inline-flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 7px 10px; margin-top: 22px; color: var(--texto-secundario); font: .62rem ui-monospace, SFMono-Regular, Consolas, monospace; letter-spacing: .055em; }
        .contact-disponibilidad > span { width: 7px; height: 7px; border-radius: 50%; background: var(--exito, #85cbb4); box-shadow: 0 0 9px var(--exito, #85cbb4); animation: pulso-disponibilidad 2.2s ease-in-out infinite; }
        .contact-disponibilidad strong { color: var(--texto-principal); font-weight: 600; }
        .contact-disponibilidad small { color: var(--texto-secundario); font: inherit; }
        .contact-acciones { display: flex; justify-content: center; flex-wrap: wrap; gap: 10px; margin-top: 20px; }
        .contact-accion-principal, .contact-accion-secundaria { display: inline-flex; align-items: center; justify-content: center; gap: 9px; min-height: 44px; padding: 10px 16px; border-radius: var(--radio-control); font-size: .8rem; font-weight: 700; text-decoration: none; }
        .contact-accion-principal { color: #07111f; background: var(--primario); }
        .contact-accion-principal:hover { color: #07111f; filter: brightness(1.08); }
        .contact-accion-secundaria { color: var(--texto-principal); border: 1px solid var(--borde-destacado); }
        .contact-accion-secundaria:hover { color: var(--primario); border-color: var(--primario); }
        .contact-option-index { color: var(--primario); font: .62rem ui-monospace, SFMono-Regular, Consolas, monospace; letter-spacing: .1em; }
        .contact-option-tags { margin: -2px 0 0; color: var(--primario); font-size: .76rem; font-weight: 700; line-height: 1.65; }
        @keyframes pulso-disponibilidad { 50% { opacity: .45; transform: scale(.72); } }
        .contact-cta-shell {
            padding: 18px 0 8px;
        }

        .contact-primary-grid {
            margin-top: 6px;
        }

        .contact-secondary-row {
            justify-content: center;
            margin-top: 2px;
        }

        .contact-card {
            position: relative;
            height: 100%;
            padding: 24px 20px 18px;
            border-radius: var(--radio-superficie);
            background: var(--fondo-tarjeta-alternativo);
            border: 1px solid var(--borde);
            box-shadow: var(--sombra-ligera);
            backdrop-filter: blur(12px);
        }

        .contact-card-primary {
            background: linear-gradient(145deg, color-mix(in srgb, var(--primario) 18%, transparent), color-mix(in srgb, var(--secundario) 10%, transparent)), var(--fondo-tarjeta-alternativo);
            border-color: color-mix(in srgb, var(--primario) 40%, var(--borde));
            box-shadow: 0 22px 45px color-mix(in srgb, var(--primario) 24%, transparent);
        }

        .contact-card-secondary {
            opacity: 0.96;
        }

        .contact-secondary-label {
            margin-bottom: 12px;
            color: var(--texto-tenue);
            font-size: 0.72rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            text-align: center;
        }

        .contact-options-grid {
            margin-top: 14px;
        }

        .contact-option-card {
            display: flex;
            flex-direction: column;
            gap: 16px;
            height: 100%;
        }

        .contact-option-head {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .contact-option-icon {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 46px;
            height: 46px;
            border-radius: var(--radio-medio);
            color: #8feaff;
            font-size: 1.25rem;
            background: linear-gradient(145deg, rgba(32, 148, 255, 0.18), rgba(0, 212, 255, 0.08));
            border: 1px solid rgba(32, 148, 255, 0.35);
            box-shadow: 0 10px 24px rgba(32, 148, 255, 0.15);
            flex-shrink: 0;
        }

        .contact-option-title {
            margin: 0;
            color: #fff;
            font-size: 1.15rem;
            font-weight: 700;
            letter-spacing: 0.01em;
        }

        .contact-option-desc {
            margin: 0;
            color: var(--texto-secundario);
            line-height: 1.6;
            font-size: 0.95rem;
        }

        .contact-option-links {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: auto;
        }

        .contact-channel {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 9px 14px;
            border-radius: 999px;
            color: #8feaff;
            font-weight: 600;
            font-size: 0.9rem;
            text-decoration: none;
            background: rgba(32, 148, 255, 0.08);
            border: 1px solid rgba(32, 148, 255, 0.3);
            transition: color 0.2s ease, background 0.2s ease, transform 0.2s ease, border-color 0.2s ease;
        }

        .contact-channel:hover {
            color: #fff;
            background: rgba(32, 148, 255, 0.2);
            border-color: rgba(32, 148, 255, 0.55);
            transform: translateY(-2px);
        }

        html[data-theme='light'] .contact-option-title {
            color: var(--texto-principal);
        }

        html[data-theme='light'] .contact-option-icon {
            color: var(--primario);
            background: linear-gradient(145deg, rgba(20, 142, 234, 0.12), rgba(57, 194, 221, 0.08));
            border-color: rgba(20, 142, 234, 0.22);
            box-shadow: 0 10px 24px rgba(20, 142, 234, 0.1);
        }

        html[data-theme='light'] .contact-channel {
            color: var(--primario);
            background: rgba(20, 142, 234, 0.07);
            border-color: rgba(20, 142, 234, 0.22);
        }

        html[data-theme='light'] .contact-channel:hover {
            color: var(--primario-oscuro);
            background: rgba(20, 142, 234, 0.12);
            border-color: rgba(20, 142, 234, 0.4);
        }

        .site-footer {
            background: linear-gradient(180deg, rgba(10, 18, 27, 0.98), rgba(8, 14, 22, 1));
            padding: 64px 0 34px;
            border-top: 1px solid rgba(32, 148, 255, 0.18);
        }

        .footer-brand {
            display: flex;
            align-items: flex-start;
            gap: 14px;
        }

        .footer-brand-mark {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 46px;
            height: 46px;
            border-radius: var(--radio-medio);
            color: #8feaff;
            background: linear-gradient(145deg, rgba(32, 148, 255, 0.18), rgba(0, 212, 255, 0.08));
            border: 1px solid rgba(32, 148, 255, 0.35);
            box-shadow: 0 10px 24px rgba(32, 148, 255, 0.15);
        }

        .footer-title,
        .footer-heading {
            color: #fff;
            margin: 0 0 12px;
        }

        .footer-title {
            font-size: 1.2rem;
        }

        .footer-heading {
            font-size: 1rem;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }

        .footer-copy,
        .privacy-policy,
        .footer-note,
        .footer-bottom-line {
            color: rgba(255, 255, 255, 0.72);
            line-height: 1.7;
            margin: 0;
        }

        .footer-links {
            display: grid;
            gap: 10px;
        }

        .footer-links a {
            color: #8feaff;
            text-decoration: none;
            transition: color 0.2s ease, transform 0.2s ease;
        }

        .footer-links a:hover {
            color: #fff;
            transform: translateX(4px);
        }

        .footer-note {
            margin-top: 14px;
            font-weight: 600;
            color: rgba(143, 234, 255, 0.9);
        }

        .privacy-policy-link {
            margin: 10px 0 0;
        }

        .privacy-policy-link a {
            color: #8feaff;
            font-weight: 700;
            text-decoration: underline;
        }

        .privacy-policy-link a:hover {
            color: #fff;
        }

        .footer-bottom {
            display: flex;
            justify-content: space-between;
            gap: 16px;
            margin-top: 34px;
            padding-top: 22px;
            border-top: 1px solid rgba(255, 255, 255, 0.08);
        }

        .copyright {
            color: #fff;
            font-weight: 600;
        }

        .contact-badge {
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            padding: 6px 14px;
            border-radius: 999px;
            background: linear-gradient(135deg, #2094ff 0%, #00d4ff 100%);
            color: #fff;
            font-size: 0.8rem;
            font-weight: 700;
            letter-spacing: 0.04em;
            text-transform: uppercase;
            box-shadow: 0 10px 24px rgba(32, 148, 255, 0.28);
        }

        html[data-theme='light'] .contact-card {
            background: linear-gradient(180deg, rgba(255, 255, 255, 0.97), rgba(247, 251, 255, 0.92));
            border-color: rgba(20, 142, 234, 0.12);
            box-shadow: 0 18px 34px rgba(15, 23, 42, 0.08);
        }

        html[data-theme='light'] .contact-card-primary {
            background: linear-gradient(145deg, rgba(20, 142, 234, 0.13), rgba(57, 194, 221, 0.08)), rgba(255, 255, 255, 0.98);
            box-shadow: 0 20px 42px rgba(20, 142, 234, 0.14);
        }

        html[data-theme='light'] .site-footer {
            background: linear-gradient(180deg, #edf5fc, #e8f1f9);
            border-top: 1px solid rgba(20, 142, 234, 0.12);
        }

        html[data-theme='light'] .footer-brand-mark {
            color: var(--primario);
            background: linear-gradient(145deg, rgba(20, 142, 234, 0.12), rgba(57, 194, 221, 0.08));
            border-color: rgba(20, 142, 234, 0.22);
            box-shadow: 0 10px 24px rgba(20, 142, 234, 0.1);
        }

        html[data-theme='light'] .footer-title,
        html[data-theme='light'] .footer-heading,
        html[data-theme='light'] .copyright {
            color: var(--texto-principal);
        }

        html[data-theme='light'] .footer-copy,
        html[data-theme='light'] .privacy-policy,
        html[data-theme='light'] .footer-note,
        html[data-theme='light'] .footer-bottom-line {
            color: var(--texto-secundario);
        }

        html[data-theme='light'] .footer-links a {
            color: var(--primario);
        }

        html[data-theme='light'] .contact-privacy-note a,
        html[data-theme='light'] .privacy-policy-link a {
            color: var(--primario);
        }

        html[data-theme='light'] .footer-links a:hover {
            color: var(--primario-oscuro);
        }

        html[data-theme='light'] .privacy-policy-link a:hover {
            color: var(--primario-oscuro);
        }

        html[data-theme='light'] .footer-bottom {
            border-top-color: rgba(20, 142, 234, 0.1);
        }

        @media (max-width: 991px) {
            .contact-card {
                max-width: 420px;
                margin: 0 auto;
            }

            .footer-bottom {
                flex-direction: column;
            }
        }
        @media (max-width: 767px) { .contact-about { grid-template-columns: 1fr; gap: 20px; padding: 20px; } .contact-about-identidad { flex-direction: row; justify-content: flex-start; text-align: left; } .contact-about-identidad img { width: 70px !important; height: 70px !important; max-width: 70px !important; flex-basis: 70px; } .contact-about-copy h3 { font-size: 1rem; } }
        @media (prefers-reduced-motion: reduce) { .contact-disponibilidad > span { animation: none; } }
    </style>
    <footer class="site-footer">
        <div class="container">
            <div class="row g-4 align-items-start">
                <div class="col-lg-4 col-md-6">
                    <div class="footer-brand">
                        <div class="footer-brand-mark"><i class="fa fa-code" aria-hidden="true"></i></div>
                        <div>
                            <h3 class="footer-title">Alex Salinas Ponce</h3>
                            <p class="footer-copy">Ingeniero de Software · Full Stack · Infraestructura &amp; DevOps enfocado en sistemas institucionales, interoperabilidad y operación tecnológica.</p>
                        </div>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <h4 class="footer-heading">Navegación rápida</h4>
                    <div class="footer-links">
                        <a href="#impacto">Impacto</a>
                        <a href="#portfolio">Portafolio</a>
                        <a href="#services">Servicios</a>
                        <a href="#contact">Contacto</a>
                    </div>
                </div>
                <div class="col-lg-4">
                    <h4 class="footer-heading">Privacidad</h4>
                    <p class="privacy-policy">Este sitio no utiliza cookies de rastreo ni recopila datos personales mediante formularios. Al cargarlo se usan recursos de terceros (Google Fonts y Bootstrap CDN).</p>
                    <p class="privacy-policy-link"><a href="https://alex81459.github.io/Alex/privacidad/">Política de Privacidad</a></p>
                    <p class="footer-note">Respuesta rápida por correo y Telegram.</p>
                </div>
            </div>
            <div class="footer-bottom">
                <span class="copyright">Copyright © Alex Salinas Ponce 2026</span>
                <span class="footer-bottom-line">Desarrollado con enfoque en claridad, rendimiento y experiencia de usuario.</span>
            </div>
        </div>
    </footer>
	
