    <header class="masthead parallax-header">
        <div class="parallax-bg"></div>
        <div class="container hero-container">
            <!-- Columna izquierda: contenido principal -->
            <div class="hero-left">
                <span class="hero-tag">INGENIERO TI · DESARROLLADOR FULL STACK</span>
                <h1 class="hero-title">Desarrollo sistemas que <span class="hero-highlight">digitalizan procesos</span> y mejoran operaciones.</h1>
                <p class="hero-desc">Construyo plataformas institucionales, APIs, automatizaciones e infraestructura tecnológica para organizaciones públicas y privadas.</p>
                <div class="hero-buttons">
                    <a class="btn btn-hero-gradient js-scroll-trigger" role="button" href="#portfolio">Ver proyectos</a>
                    <a class="btn btn-hero-outline" role="button" href="assets/curriculum-v3-2026.pdf" target="_blank" rel="noopener">
                        <i class="fa fa-download me-2"></i>Descargar CV
                    </a>
                </div>
                <div class="hero-stats-row">
                    <span class="hero-stat"><strong>18.000+</strong> interacciones API/año</span>
                    <span class="hero-stat-sep">│</span>
                    <span class="hero-stat"><strong>1.200+</strong> casos de soporte</span>
                    <span class="hero-stat-sep">│</span>
                    <span class="hero-stat"><strong>3 h → 15 min</strong> en procesos documentales</span>
                </div>
            </div>

            <!-- Columna derecha: composición visual -->
            <div class="hero-right">
                <div class="hero-visual">
                    <!-- Fondo degradado -->
                    <div class="hero-photo-bg"></div>
                    <!-- Foto rectangular -->
                    <img src="assets/img/frontalv2-circular.webp" alt="Alex Salinas Ponce - Desarrollador Full Stack" class="hero-photo">
                    <!-- Terminal compacta superpuesta -->
                    <div class="hero-terminal" aria-hidden="true">
                        <div class="hero-terminal-dots">
                            <span></span><span></span><span></span>
                        </div>
                        <div class="hero-terminal-body">
                            <span class="hero-terminal-prompt">alex@portfolio:~$</span>
                            <span class="hero-terminal-line">> Desarrollo Full Stack</span>
                            <span class="hero-terminal-line">> Automatización e integración</span>
                            <span class="hero-terminal-line">> Infraestructura TI</span>
                            <span class="hero-terminal-cursor">▌</span>
                        </div>
                    </div>
                    <!-- Tarjeta flotante 1 -->
                    <div class="hero-float-card hero-float-card-1">
                        <span class="hero-float-number">70+</span>
                        <span class="hero-float-label">establecimientos</span>
                        <span class="hero-float-sub">Infraestructura y sistemas</span>
                    </div>
                    <!-- Tarjeta flotante 2 -->
                    <div class="hero-float-card hero-float-card-2">
                        <span class="hero-float-number">15+</span>
                        <span class="hero-float-label">procesos digitalizados</span>
                        <span class="hero-float-sub">En producción</span>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <style>
        .hero-title {
            color: var(--text-main);
            font-size: clamp(2rem, 4.5vw, 3.2rem);
            font-weight: 800;
            line-height: 1.15;
            margin-bottom: 1.2rem;
            letter-spacing: -0.02em;
        }

        .hero-highlight {
            background: linear-gradient(90deg, var(--primary), #67e8f9);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-tag {
            display: inline-block;
            color: var(--primary);
            font-size: 0.78rem;
            font-weight: 700;
            letter-spacing: 0.12em;
            margin-bottom: 1.4rem;
            padding: 6px 14px;
            border: 1px solid rgba(56, 189, 248, 0.25);
            border-radius: 999px;
            background: rgba(56, 189, 248, 0.08);
        }

        .hero-desc {
            color: var(--text-main);
            font-size: 1.08rem;
            line-height: 1.65;
            max-width: 540px;
            margin-bottom: 2rem;
        }

        .hero-buttons {
            display: flex;
            gap: 18px;
            flex-wrap: wrap;
            margin-bottom: 2rem;
        }

        .btn-hero-outline {
            background: transparent;
            color: var(--primary);
            font-weight: 700;
            border: 2px solid var(--primary);
            border-radius: 18px;
            height: 56px;
            width: 205px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0 28px;
            transition: background 0.3s, color 0.3s, transform 0.2s;
        }

        .btn-hero-outline:hover {
            background: var(--primary);
            color: var(--bg-main);
            transform: translateY(-2px);
        }

        .hero-stats-row {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            color: var(--text-secondary);
            font-size: 0.88rem;
        }

        .hero-stat strong {
            color: var(--text-main);
        }

        .hero-stat-sep {
            color: var(--border);
        }

        /* Columna derecha */
        .hero-visual {
            position: relative;
            width: 300px;
            height: 510px;
            margin: 0 62px 0 auto;
            isolation: isolate;
        }

        .hero-photo-bg {
            position: absolute;
            top: 20px;
            right: -10px;
            bottom: 120px;
            left: 40px;
            border-radius: 28px;
            background: linear-gradient(145deg, var(--primary), var(--secondary));
            opacity: 0.25;
            filter: blur(40px);
        }

        .hero-photo {
            position: relative;
            z-index: 2;
            width: 100%;
            height: 370px;
            object-fit: cover;
            object-position: top;
            border-radius: 24px;
            border: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 24px 60px rgba(0, 0, 0, 0.45);
        }

        .hero-terminal {
            position: absolute;
            z-index: 3;
            top: 386px;
            bottom: auto;
            left: 0;
            right: 0;
            background: rgba(7, 17, 31, 0.94);
            border: 1px solid rgba(56, 189, 248, 0.14);
            border-radius: 10px;
            padding: 12px 14px 14px;
            font-family: 'Courier New', Consolas, Monaco, monospace;
            font-size: 0.8rem;
            line-height: 1.7;
            box-shadow: 0 12px 32px rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(10px);
        }

        .hero-terminal-dots {
            display: flex;
            gap: 6px;
            margin-bottom: 8px;
        }

        .hero-terminal-dots span {
            width: 8px;
            height: 8px;
            border-radius: 50%;
        }

        .hero-terminal-dots span:nth-child(1) { background: #ff5f57; }
        .hero-terminal-dots span:nth-child(2) { background: #ffbd2e; }
        .hero-terminal-dots span:nth-child(3) { background: #28c840; }

        .hero-terminal-prompt {
            color: var(--success);
            font-weight: 700;
        }

        .hero-terminal-line {
            display: block;
            color: var(--primary);
        }

        .hero-terminal-cursor {
            color: var(--primary);
            animation: heroCursorBlink 0.85s steps(1, end) infinite;
        }

        @keyframes heroCursorBlink {
            0%, 46% { opacity: 1; }
            47%, 100% { opacity: 0; }
        }

        .hero-float-card {
            position: absolute;
            z-index: 4;
            background: rgba(16, 30, 49, 0.92);
            border: 1px solid rgba(56, 189, 248, 0.18);
            border-radius: 14px;
            padding: 14px 16px;
            backdrop-filter: blur(12px);
            box-shadow: 0 12px 32px rgba(0, 0, 0, 0.35);
            display: flex;
            flex-direction: column;
            width: 158px;
            min-height: 108px;
            justify-content: center;
            overflow: hidden;
        }

        .hero-float-card::before {
            content: '';
            position: absolute;
            inset: 0 auto 0 0;
            width: 3px;
            background: linear-gradient(180deg, var(--primary), var(--secondary));
        }

        .hero-float-card-1 {
            top: 20px;
            right: -108px;
        }

        .hero-float-card-2 {
            right: -108px;
            top: 224px;
            bottom: auto;
        }

        .hero-float-number {
            color: var(--primary);
            font-size: 1.5rem;
            font-weight: 900;
            line-height: 1;
            letter-spacing: -0.03em;
        }

        .hero-float-label {
            color: var(--text-main);
            font-size: 0.82rem;
            font-weight: 700;
            margin-top: 2px;
        }

        .hero-float-sub {
            color: var(--text-secondary);
            font-size: 0.7rem;
            margin-top: 2px;
        }

        @media (max-width: 1199px) {
            .hero-visual {
                margin-right: 42px;
            }

            .hero-float-card-1,
            .hero-float-card-2 {
                left: auto;
                right: -78px;
            }
        }

        @media (max-width: 991px) {
            .hero-visual {
                margin: 0 auto;
            }

            .hero-float-card {
                width: 142px;
                min-width: 0;
                min-height: 96px;
            }

            .hero-float-card-1 {
                top: 18px;
                left: auto;
                right: -72px;
            }

            .hero-float-card-2 {
                left: auto;
                right: -72px;
                top: 196px;
                bottom: auto;
            }
        }

        @media (max-width: 575px) {
            .hero-container {
                padding: 112px 20px 42px !important;
                gap: 38px;
            }

            .hero-left {
                order: 0;
                width: 100%;
            }

            .hero-right {
                order: 1;
                width: 100%;
                padding-top: 4px;
            }

            .hero-tag {
                max-width: 100%;
                margin-bottom: 1.1rem;
                padding: 6px 11px;
                font-size: 0.66rem;
                letter-spacing: 0.08em;
                white-space: normal;
            }

            .hero-title {
                font-size: clamp(1.75rem, 8.5vw, 2.15rem);
                line-height: 1.12;
                margin-bottom: 1rem;
            }

            .hero-desc {
                font-size: 0.98rem;
                line-height: 1.65;
                margin-bottom: 1.6rem;
            }

            .hero-buttons {
                width: 100%;
                flex-direction: column;
                align-items: center;
                gap: 14px;
                margin-bottom: 1.8rem;
            }

            .hero-buttons .btn {
                width: min(100%, 260px);
                margin: 0;
            }

            .hero-stats-row {
                flex-direction: column;
                align-items: center;
                gap: 8px;
                margin-bottom: 0;
            }

            .hero-stat-sep {
                display: none;
            }

            .hero-visual {
                width: min(220px, 62vw);
                height: 430px;
                margin: 0 auto;
            }

            .hero-photo {
                height: 270px;
            }

            .hero-photo-bg {
                bottom: 112px;
            }

            .hero-float-card {
                width: calc(50% - 5px);
                min-height: 78px;
                padding: 8px 8px 8px 11px;
            }

            .hero-float-card-1 {
                top: 218px;
                left: 0;
                right: auto;
            }

            .hero-float-card-2 {
                top: 218px;
                left: auto;
                right: 0;
                bottom: auto;
            }

            .hero-terminal {
                top: 310px;
                left: 0;
                right: 0;
                bottom: auto;
            }
        }

        @media (max-width: 380px) {
            .hero-title {
                font-size: 1.7rem;
            }
        }
    </style>
